# 208 · Tareas

- [x] R2/R3/R4 test rojo `comandas-kanban.anuladas.test.tsx` → sección + exclusión de columnas/saturación/alerta
- [x] R1 `local-query.ts`: tercera query + `cancelled_reason`; `timezone` desde `loadComandas` y `getComandasTabData`
- [x] `local-shell.tsx` pasa `timezone` al kanban
- [x] `pnpm typecheck && pnpm test` — typecheck verde; unitarios verdes (+5 tests nuevos, incluido filtro de salón por revisión). Integración no corrió: Supabase local caído (`ECONNREFUSED :54321`), mismas 81 fallas que sin el cambio
- [ ] Verificado en vivo con el rol real

- [x] Revisión fresca (code-reviewer): sin bloqueantes
